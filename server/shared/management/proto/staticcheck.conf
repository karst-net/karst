# protoc-gen-go still emits protoimpl.MessageInfo.Exporter, which upstream
# marks deprecated (SA1019) but hasn't actually removed. Every message in
# this generated package trips it; there's nothing to fix here short of
# regenerating with a newer protoc-gen-go, which is a separate decision.
checks = ["inherit", "-SA1019"]
