# protoc-gen-go still emits protoimpl.MessageInfo.Exporter, which upstream
# marks deprecated (SA1019) but hasn't actually removed. Nothing to fix here
# short of regenerating with a newer protoc-gen-go, which is a separate
# decision.
checks = ["inherit", "-SA1019"]
