Canonical licence texts, named by SPDX identifier.

**Fetched, never transcribed by hand** — `just licenses` (or
`scripts/fetch-licenses.sh`) pulls them from spdx.org and gnu.org. A licence
text retyped or reflowed is no longer the licence it claims to be, so a diff
here should only ever appear when upstream changes one.

They are committed rather than fetched on demand. That is a change from the
original arrangement, which gitignored them: it left a public repository
showing no licence at all, and it broke the REUSE layout these filenames exist
to satisfy.

See ../LICENSE for which licence applies where, and LICENSING.md for why.
